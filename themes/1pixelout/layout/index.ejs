<%- partial('_partial/article', {post: page.posts.first(), index: true}) %>

<% if (page.posts.length > 1) { %>
<nav class="o-pagination">
  <a href="<%- url_for('archives') %>" class="o-pagination__link">Browse posts</a>
</nav>
<% } %>
